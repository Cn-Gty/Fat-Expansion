% clear all;% close all; clc;%% NOTE ********% load all_data_2d.mat and n_threeD_data.mat first%% get 3d data from file (can load from all_data_2d.mat directly)file_path = 'C:\Users\LENOVO\Desktop\dissertation\code\3d.txt';all_data = load(file_path);% save('all_data_2d','all_data')%% size of 3d matrix% num = 500;% height = 245000;% width = 265;% % %% read a certain slice% sliceNo = 499;% x = zeros(490,265);% for i = 1:490%     x(i,:) = all_data(((sliceNo-1)*490+i),:);% end% n_x = x./80;    % data type of n_x : double % % imshow(n_x)% % %% Create new void matrix% % threeD_data = zeros(490,265,500);% % %% turn them into 3D form% % Can load from file n_threeD_data directly% % for i = 1:num% %     for j = 1:490% %         threeD_data(j,:,i) = all_data(((i-1)*490+j),:);% %     end% % end% % n_threeD_data = threeD_data./80;% % % imshow(n_threeD_data);% % %% plot 3D First version% figure();% [x,y,z]=meshgrid(1:265,1:490,1:500);% % h = slice(x,y,z,n_threeD_data,132,245,[]);   % % x,y,z means the vector of the output bar, threeD_data is the data file,% % first [] means do no slice on the x domain ( front side of the body) % % **** but the front vision seems upside down *****% % second number means make a slice on the 490 axis ( side view of the body)% % third number means take one slice( this means one MRI image)`% % % test % % h = slice(x,y,z,n_threeD_data,[],[],[100,300,500]); % upside down% % set(h,'edgecolor','none')% % %% Plot 3D Second Version, Try to fix upside down problem (fixed)% % inv_threeD_data = zeros(490,265,500);% % for k = 1:500% %     inv_threeD_data(:,:,k) = n_threeD_data(:,:,501-k);% % end% % h = slice(x,y,z,inv_threeD_data,132,[],[]); % front side% % h = slice(x,y,z,inv_threeD_data,[],245,[]); % side view% % set(h,'edgecolor','none')% % %% try to build a 3D model ( not possible )